syntax = "proto3";

message ClientPulse {
  uint64 timestamp = 1;
}
message ServerPulse {
  uint64 timestamp = 1;
  // This is the interval in milliseconds that the client should wait before sending another ping.
  uint32 pingInterval = 2;
}