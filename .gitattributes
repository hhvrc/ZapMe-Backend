# Set the default behavior, in case people don't have core.autocrlf set.
* text=auto

# Windows files that need CRLF line endings.
*.bat text eol=crlf
*.sln text eol=crlf

# Linux files that need LF line endings.
*.sh text eol=lf

# Denote all files that are truly binary and should not be modified.
*.png binary
*.jpg binary
*.ico binary